import { Routes } from '@angular/router';
import { PhiMask } from './components/phi-mask/phi-mask';

export const routes: Routes = [
  {
    title: 'PHI Masking Tool',
    path: '',
    component: PhiMask,
  },
];
